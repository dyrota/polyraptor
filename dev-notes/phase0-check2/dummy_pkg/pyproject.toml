[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "polyraptor-dummy"
version = "0.0.1"
description = "Throwaway package for testing Pyodide + micropip wheel loading before touching the real polysearch/polysort libraries."
requires-python = ">=3.9"
